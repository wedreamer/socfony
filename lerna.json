{
  "packages": [
    "packages/*",
    "tencent-cloud/*",
    "error-code"
  ],
  "version": "0.0.0",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish %s"
    },
    "bootstrap": {
      "npmClientArgs": ["--no-package-lock"]
    }
  }
}
