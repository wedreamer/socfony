import { Command, Application } from "@bytegem/cloudbase";

export class PostCommand extends Command {

    handle(app: Application, data?: any) {
        throw new Error("Method not implemented.");
    }
}